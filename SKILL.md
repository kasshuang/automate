# 🖥️ pc-automation-skill
> 通用电脑自动化Skill框架 · 像安装技能一样执行任务

## 核心理念

```
用户指令 → 任务拆解 → Agent协作执行 → 结果反馈
```

## 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    🎯 任务层 (Tasks)                    │
│  通用任务 → 电商任务 → 内容任务 → 社交任务 → 自定义    │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                   🔧 执行层 (Executors)                  │
│  Browser Executor │ Desktop Executor │ File Executor     │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                   🛠️  接口层 (Interface)                │
│  Mouse │ Keyboard │ Screenshot │ OCR │ Window │ Clipboard │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                 💻 系统层 (System)                      │
│        CDP (Browser) │ PyAutoGUI │ PyWinAuto            │
└─────────────────────────────────────────────────────────┘
```

## 已实现能力

| 模块 | 原子操作 | 状态 |
|------|---------|------|
| 浏览器 | 打开URL、点击元素、填表单、截图 | ✅ |
| 桌面 | 点击坐标、键入文字、截图、OCR识别 | ✅ |
| 文件 | 读取、写入、复制、移动 | ✅ |
| 剪贴板 | 复制、粘贴、获取内容 | ✅ |

## 已实现场景任务

| 任务 | 描述 | 适用场景 |
|------|------|---------|
| `publish_article` | 发布公众号文章 | 内容运营 |
| `list_xianyu_item` | 闲鱼上架商品 | 电商 |
| `fill_form` | 批量填写表单 | 办公自动化 |

## 待扩展场景

| 场景 | 计划实现 |
|------|---------|
| 电商运营 | 闲鱼/拼多多批量上架 |
| 内容运营 | 公众号/小红书/知乎发布 |
| 社交自动化 | 微信/钉钉消息发送 |
| 数据采集 | 批量抓取网页数据 |
| 办公自动化 | 批量填表/文件处理 |

## 使用方式

### 方式1：通过我（主Agent）执行
```
少主：帮我发布一篇文章到公众号
我：   理解任务 → 调用 publish_article 任务
      → 自动规划步骤 → 执行 → 汇报结果
```

### 方式2：直接调用Skill
```
用户指令："安装电商运营技能"
我：   安装电商场景扩展包
用户："上架闲鱼商品：标题xxx 价格xxx"
我：   调用 list_xianyu_item 任务执行
```

## 任务描述语言

```json
{
  "task": "publish_article",
  "params": {
    "title": "文章标题",
    "content": "文章内容",
    "platform": "公众号/小红书/知乎"
  }
}
```

## 文件结构

```
pc-automation-skill/
├── SKILL.md                 # Skill定义文件
├── README.md               # 使用文档
├── core/
│   ├── __init__.py
│   ├── interface.py         # 统一接口
│   ├── browser.py           # 浏览器控制
│   ├── desktop.py           # 桌面控制
│   └── clipboard.py         # 剪贴板
├── tasks/
│   ├── __init__.py
│   ├── base.py             # 任务基类
│   ├── publish_article.py   # 发布文章
│   └── list_xianyu_item.py  # 闲鱼上架
└── examples/
    └── demo.py             # 使用示例
```
